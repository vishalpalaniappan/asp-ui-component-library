import "./TraceDiagram.scss";
import PropTypes from 'prop-types';

/**
 * Renders the trace diagram.
 * 
 * @param {Array} traces 
 * @return {JSX}
 */
export const TraceDiagram = ({traces}) => {
    
    return (
        <div>
            {"asdf"}
        </div>
    );
}

TraceDiagram.propTypes = {
    traces: PropTypes.array
}