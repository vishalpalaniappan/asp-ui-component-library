import "./Tabs.scss";
import PropTypes from 'prop-types';

/**
 * Renders the tab component.
 * 
 * @return {JSX}
 */
export const Tabs = ({}) => {
    
    return (
        <>
            
        </>
    );
}

Tabs.propTypes = {
}